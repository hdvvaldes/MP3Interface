
## Backend

Language: **Rust**

Files Description:
- data_miner.rs: ADS to model deciphering the information off a mp3 files directory.
- song_repo.rs: Interface with the information. (In this case off of a database).

**General Purpose:**
- file_manager.rs: Write to files and read files.
- data_types.rs: Types declaration. 
    - Song 
    - Artist 
    - Album


```mermaid
classDiagram
    class DataMiner {
        +mine(directory: String) Song[]
    }
    class SongRepo {
        +save(song: Song)
        +get_all() Song[]
    }
    class FileManager {
        +read(path: String) String
        +write(path: String, content: String)
    }
    class DataTypes {
        +Song
        +Artist
        +Album
    }
```

## API

Language: Python

Optimally its going to generate the doc

```mermaid
classDiagram
    class Endpoint{
        +add_songs(songs: iterable)
        +get_songs(filter: func)
        +update_song(song: Song)
        +delete_songs(songs: iterable)
        +get_recommendations()
    }
```

## FrontEnd

Language: Flutter

NOTES/TODO: 
- how to implement responses and requests


**Method Description**: 
+requireArgs(N: int) String[]: Check for exactly N arguments


```mermaid
classDiagram
    class View{
        +display_welcome()
        +display_prompt()
        +display_error(message: String)
        +display_song(song: Song)
        +display_player()
    }
    note "Lexer is generated by flex++"
    class Lexer

    class Controller{
        +requireArgs(N: int)
        +send_request(request: Request) Response
        -handle_response(response: Response) 
    }
    
    note "I'll try to omit this class using flex++ capabilities"
    class Parser{

    }

```

